PAYMENT_DEBUG=false
# Production: point both website and bot to the same Postgres database.
# Example: DATABASE_URL=postgresql://user:pass@host:5432/quizpathshala
DATABASE_URL=
# Local fallback only when DATABASE_URL is not set.
DB_PATH=
